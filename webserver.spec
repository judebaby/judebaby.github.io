# -*- mode: python -*-
a = Analysis(['website.py'],
             pathex=['D:\\misc\\SIMA\\Web'],
             hiddenimports=['_socket'],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='webserver.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='webserver')
